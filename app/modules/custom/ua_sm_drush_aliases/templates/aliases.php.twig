{#
/**
 * @file
 * Default theme implementation for rendering drush aliases.
 */
#}
<?php

// Site: {{ site_title }}

{% for environment_machine_name, environment in environments %}
// Environment: {{ environment.title }}
$aliases['{{ environment_machine_name }}'] = [
  'root' => '/web/app',
  'uri' => '{{ environment.uri }}',
  'remote-host' => '{{ environment.remote_host }}',
  'remote-user' => 'root',
  'ssh-options' => '-p {{ environment.ssh_port }}',
];

{% endfor %}
