resources:
  'entity:node':
    GET:
      supported_formats:
        - hal_json
        - json
      supported_auth:
        - cookie
        - oauth
    POST:
      supported_formats:
        - hal_json
        - json
      supported_auth:
        - oauth
    PATCH:
      supported_formats:
        - hal_json
        - json
      supported_auth:
        - oauth
  'entity:comment':
    GET:
      supported_formats:
        - hal_json
        - json
      supported_auth:
        - cookie
    POST:
      supported_formats:
        - hal_json
        - json
      supported_auth:
        - oauth
