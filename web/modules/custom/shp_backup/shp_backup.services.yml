services:
  shp_backup.backup:
    class: Drupal\shp_backup\Service\Backup
    arguments: ['@config.factory', '@token', '@entity_type.manager']
  shp_backup.settings:
    class: Drupal\shp_backup\Form\ShepherdSettings
    arguments: ['@renderer']
