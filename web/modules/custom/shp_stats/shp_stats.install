<?php

/**
 * Implements hook_install().
 */
function shp_stats_install() {

  for ($i = 0; $i < mt_rand(1, 20); $i++) {
    $month = mt_rand(1, 12);
    $year = mt_rand(10, 17);

    $fields = [
      'date' => date("Y-m-d H:i:s", strtotime($month . '/1/' . $year)),
      'tag' => 'prd',
      'count' => mt_rand(1, 20),
    ];
    db_insert('shp_stats_active')
      ->fields($fields)
      ->execute();
  }
}
/**
 * Implements hook_schema().
 */
function shp_stats_schema() {

  $schema['shp_stats_active'] = [
    'description' => 'Stores counts of active sites for a given tag by month-year.',
    'fields' => [
      'id' => [
        'type' => 'serial',
        'not null' => TRUE,
        'description' => 'Primary Key: Unique entry ID.',
      ],
      'date' => [
        'type' => 'varchar',
        'mysql_type' => 'datetime',
        'not null' => TRUE,
        'description' => 'Datetime of the entry',
      ],
      'tag' => [
        'type' => 'varchar',
        'length' => 255,
        'not null' => TRUE,
        'description' => 'Tag entry belongs to - prd, uat, etc.',
      ],
      'count' => [
        'type' => 'int',
        'not null' => TRUE,
        'default' => 0,
        'description' => 'The number of active sites',
      ],
    ],
    'primary key' => ['id'],
    'indexes' => [
      'date' => ['date'],
      'tag' => ['tag'],
    ],
  ];

  return $schema;
}
